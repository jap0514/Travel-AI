# 智能旅游规划系统 - 简历亮点增强计划

> 本文档规划如何将现有项目升级为「高并发、高可用、分布式」的企业级项目，使其在简历面试中更具竞争力。

---

## 一、现状盘点

### 1.1 已有技术栈

| 层级 | 技术 |
|------|------|
| Java后端 | Spring Boot 3.1.6 + MyBatis Plus + JWT |
| Python后端 | FastAPI + LangGraph + LangChain |
| 数据库 | MySQL 8.0 + Redis 7.0 |
| 消息队列 | RocketMQ 5.3.2 |
| 向量数据库 | Qdrant |
| 分布式锁 | Redisson 3.27.2 |
| AI能力 | DeepSeek R1 (本地Ollama部署) |

### 1.2 现有亮点
- [x] Java + Python 双后端架构
- [x] LangGraph 多Agent协作（9节点 + Critic循环）
- [x] RAG混合检索（Reranking）
- [x] WebSocket + SSE 实时推送
- [x] Redis分布式锁（订单超时处理）
- [x] RocketMQ异步解耦

### 1.3 简历缺口（常见面试问题）

| 类别 | 缺失内容 |
|------|---------|
| **高并发** | 限流、削峰、并发控制 |
| **高可用** | 熔断、降级、超时控制 |
| **缓存** | 三级缓存、缓存一致性、缓存问题防治 |
| **分布式** | 幂等性、分布式事务、幂等锁 |
| **可观测性** | 全链路追踪、Metrics、告警 |
| **安全** | 接口签名、权限控制 |
| **工程化** | 统一异常处理、统一响应结构 |

---

## 二、改造计划

### 阶段一：工程基建（1-2天）

不涉及核心业务逻辑，但能显著提升代码质量和面试评价。

---

#### 1.1 统一响应结构 + 全局异常处理

**现状**：各接口返回格式不统一，异常直接暴露。

**改造**：统一`Result<T>`包装 + `@RestControllerAdvice`全局异常。

**简历亮点**：
> - 统一响应结构设计（`Result<T>` + 状态码枚举）
> - 全局异常处理机制（业务异常、系统异常分离）
> - 异常链路追踪，接口错误友好提示

**涉及文件**：
```
travel-common/src/main/java/com/travel/common/
├── result/
│   ├── Result.java          # 统一响应包装
│   ├── ResultCode.java      # 状态码枚举
│   └── ResultUtil.java      # 响应构造工具
└── exception/
    ├── GlobalExceptionHandler.java  # @RestControllerAdvice
    ├── BusinessException.java       # 业务异常
    └── RemoteException.java         # 远程调用异常
```

---

#### 1.2 参数校验 + JSR-303

**现状**：Controller层缺少参数校验。

**改造**：在DTO字段上加`@NotBlank`、`@Min`、`@Max`等注解，全局校验。

**简历亮点**：
> - 基于JSR-303的参数自动校验机制
> - 分组校验（创建/更新场景分离）

**涉及文件**：
- `ChatMessageDTO.java`、`AiMessageCallbackDTO.java`等

---

#### 1.3 接口签名验证（安全加固）

**现状**：Java-Python通信仅用HMAC签名，功能较弱。

**改造**：升级为**双Token签名机制**（参考微信支付签名方案）。

**简历亮点**：
> - 双Token签名验证，防止请求篡改
> - 请求body完整性校验（MD5/SHA256）
> - 时间戳防重放攻击（5分钟窗口）

**实现方案**：
```
Sign = SHA256(secretKey + timestamp + body)
Header: X-App-Id, X-Timestamp, X-Sign
```

---

#### 1.4 日志规范化 + TraceId串联

**现状**：日志分散，无TraceId串联。

**改造**：引入`Logback`统一日志格式，`Filter`生成TraceId贯穿全链路。

**简历亮点**：
> - 统一日志规范（JSON格式）
> - TraceId全链路串联（Java → RocketMQ → Python）
> - 基于日志的链路分析能力

---

### 阶段二：缓存与性能（2-3天）

---

#### 2.1 三级缓存架构

**现状**：仅使用Redis，有缓存穿透风险。

**改造**：实现**本地缓存(Caffeine) → Redis → MySQL** 三级缓存。

**简历亮点**：
> - 三级缓存架构设计（本地堆内缓存 + Redis分布式缓存 + MySQL持久化）
> - 缓存击穿防护（分布式锁 + 单flight限流）
> - 缓存雪崩控制（TTL随机偏移 + 热点数据永不过期）
> - 缓存预热策略（启动时加载热点数据）

**缓存层级**：

```java
@Resource(name = "tieredCacheManager")
private CacheManager cacheManager;

// 第一层：Caffeine（进程内，1000条，5分钟）
// 第二层：Redis（分布式，10000条，30分钟）
// 第三层：MySQL（持久化）

@Cacheable(cacheNames = "hotels", key = "#city")
public List<Hotel> getHotelsByCity(String city) {
    // 三级查询：本地 → Redis → DB
}
```

**缓存更新策略**：
```java
@CachePut(cacheNames = "hotels", key = "#city")
public List<Hotel> refreshHotels(String city) {
    // 主动更新：先写DB，再删Redis，由下次请求回填
}

@CacheEvict(cacheNames = "hotels", allEntries = true)
public void evictAllHotels() {
    // 被动失效：酒店信息变更时全量清除
}
```

---

#### 2.2 热点数据识别 + 动态缓存

**现状**：缓存key固定，无热点感知能力。

**改造**：引入**HotKey检测**（Cuberoute/RedisHotKeys），对高频访问的酒店/景点动态提升缓存。

**简历亮点**：
> - 基于滑动窗口的热点数据识别算法
> - 热点Key自动降级到本地缓存，降低Redis压力
> - 动态缓存淘汰策略（LRU + LFU混合）

---

### 阶段三：高并发（3-4天）

---

#### 3.1 接口限流（RateLimiter）

**现状**：无任何限流措施。

**改造**：基于Redisson `RateLimiter`实现接口限流。

**简历亮点**：
> - 基于Redis令牌桶算法的接口限流
> - 多维度限流策略（用户维度 + IP维度 + 接口维度）
> - 限流触发后的友好提示（返回码429 + 等待时间）

**实现方案**：

```java
@RateLimiter(value = 200, timeout = 1,
             fallbackMethod = "sendMessageFallback")
public Result<String> sendMessage(SendMessageDTO dto) {
    // 限流：每秒钟200个请求
}

public Result<String> sendMessageFallback(SendMessageDTO dto, Exception e) {
    return Result.fail(ResultCode.RATE_LIMITED);
}
```

**限流配置**：

| 接口 | 限流值 | 说明 |
|------|--------|------|
| `/message/sendMessage` | 100/min | 消息发送 |
| `/hotel/order/createOrder` | 50/min | 下单 |
| `/hotel/hotelInfo/getHotelByCity` | 200/min | 查询 |

---

#### 3.2 熔断降级（CircuitBreaker）

**现状**：Python服务宕机时Java端无降级。

**改造**：引入Resilience4j，实现熔断 + 降级 + 超时控制。

**简历亮点**：
> - 引入Resilience4j熔断器（失败率50%触发熔断）
> - 熔断后的降级策略（返回缓存结果 / 友好提示）
> - Python服务调用的超时控制（3秒超时）
> - 熔断状态的自动恢复（60秒后半开尝试）

**实现方案**：

```java
@CircuitBreaker(
    name = "pythonService",
    fallbackMethod = "callPythonFallback",
    successThreshold = 3,      // 连续3次成功则关闭
    failureRateThreshold = 50,  // 50%失败率触发
    waitDurationInOpenState = 60s
)
@Timeout(name = "pythonService", duration = 3s)
public String callPythonAI(String message) {
    return restTemplate.postForObject(pythonUrl, dto, String.class);
}

public String callPythonFallback(String message, Exception e) {
    log.warn("Python服务降级，message={}", message);
    return "AI服务暂时繁忙，请稍后再试。";
}
```

---

#### 3.3 订单幂等性设计

**现状**：创建订单无幂等校验。

**改造**：基于Redis + Token实现接口幂等。

**简历亮点**：
> - 基于Redis的分布式Token幂等锁
> - 订单创建接口幂等设计（防重复提交）
> - 幂等Key的自动清理（TTL过期）

**实现方案**：

```java
@Idempotent(key = "#token", expireTime = 300, message = "请勿重复提交")
public Result<HotelBooking> createOrder(
    @RequestParam("token") String token,
    @RequestBody CreateOrderDTO dto
) {
    // 1. 检查token是否存在（不存在说明首次请求，放行）
    // 2. token存在则说明重复，直接返回原结果
}
```

**流程**：
```
1. 前端下单前，先调 /hotel/order/token 获取token
2. 下单时带token，服务端Redis中检查：
   - 不存在 → 放行，存入Redis（TTL=5分钟），执行业务
   - 存在 → 直接返回之前的结果（防重复）
```

---

#### 3.4 订单超时——完整版分布式锁

**现状**：有Redisson锁处理超时，但不够健壮。

**改造**：增强为完整的**RocketMQ延迟消息 + 分布式锁**方案。

**简历亮点**：
> - RocketMQ延迟消息实现订单超时（30分钟未支付自动取消）
> - 分布式锁抢锁机制（确保集群内仅一个节点处理超时订单）
> - 防死锁设计（锁自动过期 + 补偿队列）
> - 订单状态的幂等转换（防止重复取消）

**流程图**：

```
订单创建 → 发送RocketMQ延迟消息（30分钟后） → 消费者抢锁 → 执行业务 → 更新状态
                                  ↓
                          分布式锁（Redisson）
                                  ↓
                          检查订单状态（已支付则跳过）
```

---

#### 3.5 多级队列削峰

**现状**：请求直接处理，无缓冲。

**改造**：用户请求 → **限流** → **RocketMQ** → 异步处理 → WebSocket通知。

**简历亮点**：
> - 基于RocketMQ的异步削峰架构
> - 多消费者集群部署（提升吞吐量）
> - 消息积压监控 + 告警
> - 消息消费的幂等处理

**队列设计**：

| 队列名 | 消费者数 | 说明 |
|--------|---------|------|
| `travel-message MQ` | 5 | 聊天消息处理 |
| `travel-order MQ` | 3 | 订单处理 |
| `travel-plan MQ` | 2 | 行程规划（耗时长） |

---

### 阶段四：可观测性（1-2天）

---

#### 4.1 全链路追踪

**现状**：有Langfuse但未串联Java端。

**改造**：Java端引入**SkyWalking / Micrometer**，串联Java → RocketMQ → Python全链路。

**简历亮点**：
> - 全链路追踪体系（SkyWalking + Micrometer）
> - Java → RocketMQ → Python → LLM 完整调用链追踪
> - 关键节点的耗时分析（p99/p95/p90）
> - 分布式请求的上下文传播（TraceId透传）

**关键Span**：

```
[Java] sendMessage()                    120ms
  └─ [RocketMQ] produce                 15ms
       └─ [Python] consume              2000ms
            └─ [Python] researcher      500ms
            └─ [Python] planner         300ms
            └─ [LLM]  DeepSeek          1500ms
```

---

#### 4.2 Metrics监控 + 告警

**改造**：引入Micrometer + Prometheus + Grafana。

**简历亮点**：
> - 核心接口QPS/TPS监控
> - 缓存命中率监控
> - Python服务可用性监控
> - 告警规则配置（CPU/内存/接口耗时/错误率）

**监控大盘**：

| 指标 | 告警阈值 | 处理 |
|------|---------|------|
| Python服务错误率 | >5% | 触发熔断 |
| 消息队列积压 | >10000 | 扩容消费者 |
| 接口p99耗时 | >3s | 排查优化 |
| 缓存命中率 | <80% | 检查缓存策略 |

---

### 阶段五：高级特性（2-3天）

---

#### 5.1 分布式事务（Seata AT模式）

**现状**：订单创建与库存扣减不在同一事务。

**改造**：引入Seata，实现订单服务的分布式事务。

**简历亮点**：
> - Seata AT模式实现分布式事务
> - 订单创建 + 库存扣减 + 支付分布式事务一致性
> - 事务回滚机制演示
> - 分布式事务的隔离性保障

**场景**：
```sql
-- 订单服务（Java）
BEGIN;
INSERT INTO hotel_booking (...) VALUES (...);
UPDATE hotel_room SET available = available - 1 WHERE id = ?;
COMMIT;  -- Seata自动处理分布式事务
```

---

#### 5.2 读写分离

**现状**：所有读写操作都在主库。

**改造**：配置MySQL主从复制，查询走从库。

**简历亮点**：
> - MySQL读写分离架构（1主2从）
> - 动态数据源路由（@ReadOnly注解）
> - 主从延迟处理（读从库前先检查延迟）
> - 读写分离后的数据一致性保证

---

## 三、简历写法指南

### 3.1 项目概述写法

**初级版**（无亮点）：
> 开发了一个旅游规划系统，使用Spring Boot + MyBatis + Redis。

**升级版**（加亮点后）：
> 智能旅游规划系统是一套面向小程序用户的AI旅行规划平台，采用 Java Spring Boot 与 Python LangGraph 双后端架构设计，通过 RocketMQ 消息队列实现跨语言异步通信，日均处理请求 5000+，核心接口 p99 响应时间 < 500ms。

### 3.2 技术亮点写法（按模块）

**架构设计**：
> - 设计并实现 Java + Python 双后端架构，通过 HTTP 回调 + RocketMQ 实现跨语言通信，服务解耦度 > 80%
> - 基于领域驱动设计（DDD）划分模块，核心业务与基础设施分层治理

**AI能力**：
> - 基于 LangGraph 构建多 Agent 协作系统，包含 9 个专业节点（Researcher/Planner/Critic等），通过循环评审机制优化行程方案
> - 引入 Qdrant 向量数据库实现 RAG 混合检索，结合 CrossEncoder Reranking 提升检索质量

**高并发与性能**：
> - 设计三级缓存架构（本地 Caffeine + Redis + MySQL），缓存命中率从 60% 提升至 92%
> - 基于 Redis RateLimiter 实现多维度限流（用户/IP/接口），核心接口承载能力提升 5 倍
> - 引入 Resilience4j 熔断器，实现 Python AI 服务的超时控制与降级策略，服务可用性 > 99.5%
> - RocketMQ 延迟消息实现订单超时处理，分布式锁确保集群环境下订单幂等性

**分布式与缓存**：
> - 实现分布式幂等性设计（Redis Token 机制），解决重复提交问题
> - 缓存击穿/雪崩/穿透问题的系统性防治（分布式锁 + TTL随机化 + 热点数据降级）
> - Redisson 分布式锁实现集群环境下的资源竞争控制

**可观测性**：
> - 搭建全链路追踪体系（SkyWalking + Micrometer），串联 Java → RocketMQ → Python → LLM 完整调用链
> - 配置多级告警规则（QPS/错误率/p99耗时/队列积压），实现故障的早发现早处理

**安全与工程**：
> - 实现双 Token 签名验证 + 时间戳防重放机制，保障 Java-Python 通信安全
> - 统一响应结构 + 全局异常处理，接口一致性 100%

### 3.3 面试加分项

| 加分项 | 面试话术 |
|--------|---------|
| **三级缓存** | "我设计了一套三级缓存，热点数据直接走进程内缓存，避免每次都查Redis" |
| **熔断降级** | "当Python服务挂了，前端会显示'AI繁忙'而不是一直在转圈" |
| **幂等性** | "下单接口加了幂等token，用户快速点击只会有一个订单" |
| **全链路追踪** | "用户报障时，我能在SkyWalking里还原完整调用链，定位到是哪个环节慢了" |
| **分布式锁** | "取消订单时用Redisson锁，防止集群里多个节点同时处理" |

---

## 四、优先级与时间规划

| 阶段 | 内容 | 工期 | 简历效果 | 优先级 |
|------|------|------|---------|--------|
| **阶段一** | 工程基建（统一响应/异常/日志） | 1-2天 | ⭐⭐ | P0 |
| **阶段二** | 三级缓存架构 | 2-3天 | ⭐⭐⭐⭐ | P0 |
| **阶段三** | 限流 + 熔断 + 幂等性 | 3-4天 | ⭐⭐⭐⭐⭐ | P0 |
| **阶段四** | 全链路追踪 + 监控 | 1-2天 | ⭐⭐⭐⭐ | P1 |
| **阶段五** | 分布式事务 + 读写分离 | 2-3天 | ⭐⭐⭐ | P2 |

> **P0 为必做项**，总计约 **6-9天**，可涵盖简历80%的亮点问题。

---

## 五、快速启动清单

### Day 1-2：工程基建
- [ ] 创建 `Result.java` 统一响应类
- [ ] 创建 `GlobalExceptionHandler.java`
- [ ] 统一日志配置（添加TraceId Filter）
- [ ] JSR-303 注解补充到关键DTO

### Day 3-5：缓存 + 高并发
- [ ] 引入Caffeine依赖
- [ ] 实现 `TieredCacheManager`
- [ ] 改造酒店查询接口为三级缓存
- [ ] 添加 `@RateLimiter` 注解
- [ ] 添加 `@CircuitBreaker` 注解
- [ ] 实现幂等Token接口

### Day 6-7：可观测性
- [ ] 引入 Micrometer + Prometheus
- [ ] 配置 SkyWalking Agent
- [ ] 添加关键接口的 `@Timed` 注解
- [ ] 配置 Grafana 监控大盘

---

## 六、注意事项

1. **不要过度设计**：面试官会追问细节，确保每加的功能都能说清楚原理
2. **优先解决现有问题**：如果代码里有TODO注释，先解决这些
3. **保留原有亮点**：已有的LangGraph多Agent、RAG不要丢弃，继续深化
4. **量化成果**：用数字说话（命中率从X%提升到Y%，QPS从X提升到Y）
5. **准备失败案例**：准备一个"缓存穿透问题排查与解决"的真实案例

---

*文档版本：v1.0*
*创建时间：2026/07/21*
*适用项目：智能旅游规划系统*
