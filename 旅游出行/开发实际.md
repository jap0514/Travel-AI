1、第一天

完成Docker的一键式编排容器

通过JWT+Redis黑名单来完成小程序的登录和退出操作

自定义异常体系，让全局异常处理类来统一处理

使用mybatisX插件根据数据库表来生成Java实体和对应的controller、service、mapper文件





2、第二天

配置mybatis-plus的分页插件，也就是将分页插件添加到 MyBatis-Plus 的 `MybatisPlusInterceptor`（插件链）中，当执行SQL前，去插件链遍历并执行`intercept()` 方法。





3、现在把goharbor和Harbor删了，以后再下载回来



4、Java接收前端的任务数据，新增到数据库中，接着转换成JSON格式发送给Rocketmq



5、签名的设计



6、接通Python和Rocketmq，进行消息的消费和发送，Java端可以正常进行消费



7、让会话正常管理对话内容，再从对话内容里面提取出任务

（在会话里面，用户发送消息，Java接收，封装好，然后发送给Rocketmq，Python消费，将消息里面的content内容解析出任务对应的字段信息，将任务传回给Java做新增任务保存到数据库，设置状态为任务已发送未处理状态，然后Python继续根据解析到的任务信息，进行模型的调用和处理，得到模型返回结果后通过Rocketmq发送给Java，保存到数据库并修改任务状态，返回给前端）

（如果用户对返回的结果不满意）



8、新建会话的时候，后端生成 ✅



9、挑选agent框架+memory ✅



10、实现单个模型的问答 ✅



11、实现多智能体的协调  ✅



12、实现多智能体的**自我评估、循环修正**。实现自我迭代（出现多次消费同一条消息的问题）✅



13、plan进行结构化+存储 ✅



14、完善 MCP Tools / Skills 

工具：天气、价格查询、交通方式、本地知识库RAG

RAG：

1)保存一些经典、常规的旅游经典组合(比如3日游、7日游等)

2)某个经典的历史背景、文化意义、亮点特色

3)历史的一些行程规划



----完成Qdrant向量数据库的配置（id必须为整数或者UUID，不可以是其他的类型）

----MCP工具调用必须是异步操作，不可以同步

----agent里面的同步、异步、事件循环（知识点很多）

----agent中mcp服务中工具调用的准确性（通过prompt中提供工具和工具解析，让模型分析该使用哪个）

----协调心知天气返回的字段和代码里面调用的字段，确保API_KEY正确，完成天气的查询



需要完成:

RAG向量数据库的各种配置

1、检索延迟低

2、准确性高、提高有效召回率

3、提升吞吐量

4、支持混合检索

5、做到有效的元数据和标量过滤

6、支持多模态

7、实时与增量更新

8、CRUD与全生命周期管理

9、访问控制和数据加密







15、完善短期、长期记忆和个性化



16、生产级能力



7月13的工作量（有一个问题就是需要判断当前操作的是否是对应的用户或者是管理员，用户无法操作其他用户的数据）



17、

把chatmessage里面的plan_json弄成一张表 ✅

分析用户历史旅行偏好：去过哪些目的地、最常选的天数/预算/节奏

统计某个用户常去的目的地；✅

统计所有用户常去的目的地；（近7天、近30天等）✅





18、会话消息上下文查询（带分页 + 排序）

 --查询某个用户的所有会话（需要展示）✅

  -- 查询某个会话的所有消息，带发送者昵称✅



19、热门目的地统计（聚合 + 排序 + 限制）

  -- 统计平台热门目的地（近30天），用于推荐✅



20、制作酒店管理相关的表，做酒店预约✅









7月14的工作量

21、先查酒店是否有空房间预约，如果没有，再去查到旅游时间的时候是否已经退房

有房间---->返回对应的旅游规划

无房间---->提示用户，看用户是否还需要继续规划



（1）根据目的地、开始时间、旅游几天，去选择酒店，查询是否有空房间，将查到的信息返回给用户，看用户怎么选择；提供当地的有房间的酒店供用户选择，选择好之后，再结合这个预定结果生成后面的旅游规划。

（2）如果当地的酒店都没有房间，返回信息，看用户怎么选择，（制作闹钟在前面游客退房前提醒用户去抢空房间）（或者不需要房间直接做后续的行程规划）

Java：查询酒店信息✅、查询酒店房间类型信息✅、查询酒店房间信息✅、查询是否有空房间✅、生成订单✅、查询订单信息✅、修改订单信息（简化版支付处理修改状态为已支付0-1✅、临时不要了修改状态为已取消0-3（用户取消✅、超时取消✅）、退房处理修改状态为已完成0-1-2-4✅）

python：在task分析节点获得用户的目的地、开始时间、旅游几天；在researcher节点去调用Java接口查询酒店房间情况；然后返回



22、如果有房间，在做完行程规划后，确定用户需要预约酒店后，就调用工具来完成预约。



23、对酒店预约的功能做并发处理



24、python那边停了服务器，Java这边都没有察觉到



25、单体项目可以直接用new objectMapper，但是如果是微服务的话，就需要在配置类中定义一个单例，这样就不会浪费内存和CPU





