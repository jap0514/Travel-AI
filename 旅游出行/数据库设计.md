# 二、核心JSON数据结构
## 1. 行程单 content 结构（travel_plan.content）
```json
{
  "title": "北京3日精华游",
  "destination": "北京",
  "days": 3,
  "budget": "中等",
  "pace": "正常",
  "itinerary": [
    {
      "day": 1,
      "title": "抵达北京 → 天安门 → 故宫",
      "morning": "天安门广场、故宫博物院",
      "afternoon": "景山公园、南锣鼓巷",
      "evening": "后海夜景",
      "hotel": "市中心酒店",
      "food": ["北京烤鸭", "炸酱面"],
      "traffic": "地铁+步行",
      "tips": "提前预约故宫门票"
    }
  ],
  "weather": "晴，18-28℃",
  "totalCost": "人均1500元"
}
```

## 2. 系统Agent配置（sys_config）
```json
{
  "systemPrompt": "你是专业旅行规划AI...",
  "temperature": 0.7,
  "ragTopK": 5,
  "maxTokens": 3072,
  "timeoutSeconds": 60
}
```

---

# 三、接口统一返回结构（所有接口通用）
```json
{
  "code": 200,
  "message": "成功",
  "traceId": "78a2f9d011ef",
  "data": {}
}
```

## 状态码说明
- 200 成功
- 400 参数错误
- 401 未登录
- 403 无权限
- 404 资源不存在
- 429 请求频繁
- 500 服务异常

---

# 四、分页结构（所有列表接口）
```json
{
  "total": 128,
  "pageNum": 1,
  "pageSize": 10,
  "pages": 13,
  "list": []
}
```

---

# 五、WebSocket推送结构
```json
{
  "type": "PROGRESS",
  "taskId": 1001,
  "progress": 60,
  "message": "正在规划路线",
  "planId": null
}
```

----------------------------------------------------------------------------------------------------------------------------------------------------------


# 旅游AI系统 —— 全套数据表结构（字典格式 · 不含SQL · 直接写论文）
我严格按你确定的 **12 张表** 整理，全部用**字段名: {类型, 含义}** 字典格式，干净、标准、可直接复制到毕业设计。

---

## 1. 用户表（user）
```
{
  userId: {类型: bigint, 含义: 用户ID，主键},
  openId: {类型: varchar(64), 含义: 微信openId，唯一标识},
  nickname: {类型: varchar(32), 含义: 用户昵称},
  avatar: {类型: varchar(512), 含义: 头像URL},
  budget: {类型: enum, 含义: 预算偏好：低/中/高},
  pace: {类型: enum, 含义: 行程节奏：休闲/正常/紧凑},
  status: {类型: tinyint, 含义: 账号状态：0禁用 1启用},
  createTime: {类型: datetime, 含义: 创建时间},
  updateTime: {类型: datetime, 含义: 更新时间}
}
```

## 2. 角色表（role）
```
{
  roleId: {类型: bigint, 含义: 角色ID，主键},
  roleName: {类型: varchar(32), 含义: 角色名称：ADMIN/USER},
  description: {类型: varchar(64), 含义: 角色描述},
  createTime: {类型: datetime, 含义: 创建时间}
}
```

## 3. 用户角色关联表（user_role）
```
{
  id: {类型: bigint, 含义: 主键ID},
  userId: {类型: bigint, 含义: 用户ID},
  roleId: {类型: bigint, 含义: 角色ID},
  createTime: {类型: datetime, 含义: 绑定时间}
}
```

## 4. 菜单表（menu）
```
{
  menuId: {类型: bigint, 含义: 菜单ID，主键},
  name: {类型: varchar(32), 含义: 菜单名称},
  path: {类型: varchar(128), 含义: 前端路由路径},
  icon: {类型: varchar(64), 含义: 菜单图标},
  parentId: {类型: bigint, 含义: 父菜单ID，0为一级菜单},
  sort: {类型: int, 含义: 排序号},
  createTime: {类型: datetime, 含义: 创建时间}
}
```

## 5. 菜单角色关联表（role_menu）
```
{
  id: {类型: bigint, 含义: 主键ID},
  roleId: {类型: bigint, 含义: 角色ID},
  menuId: {类型: bigint, 含义: 菜单ID},
  createTime: {类型: datetime, 含义: 授权时间}
}
```

## 6. 行程任务请求表（travel_task）
```
{
  taskId: {类型: bigint, 含义: 任务ID，主键},
  userId: {类型: bigint, 含义: 创建任务的用户ID},
  userQuery: {类型: text, 含义: 用户旅行需求描述},
  days: {类型: int, 含义: 出行天数},
  budget: {类型: varchar(16), 含义: 本次预算},
  pace: {类型: varchar(16), 含义: 本次行程节奏},
  status: {类型: enum, 含义: 任务状态：INIT/QUEUED/PROCESSING/COMPLETED/FAILED/CANCELLED},
  planId: {类型: bigint, 含义: 生成的行程单ID，完成后才有值},
  errorMsg: {类型: varchar(512), 含义: 失败原因},
  traceId: {类型: varchar(64), 含义: 全链路追踪ID},
  createTime: {类型: datetime, 含义: 创建时间},
  updateTime: {类型: datetime, 含义: 更新时间}
}
```

## 7. 行程规划表（travel_plan）
```
{
  planId: {类型: bigint, 含义: 行程单ID，主键},
  taskId: {类型: bigint, 含义: 关联的任务ID},
  userId: {类型: bigint, 含义: 用户ID},
  title: {类型: varchar(128), 含义: 行程标题},
  destination: {类型: varchar(64), 含义: 目的地城市},
  days: {类型: int, 含义: 总天数},
  content: {类型: json, 含义: 结构化行程JSON：每日安排、景点、美食、交通等},
  weather: {类型: varchar(256), 含义: 天气信息},
  createTime: {类型: datetime, 含义: 生成时间}
}
```

## 8. 会话记录表（chat_session）
```
{
  sessionId: {类型: bigint, 含义: 会话ID，主键},
  userId: {类型: bigint, 含义: 用户ID},
  taskId: {类型: bigint, 含义: 关联的行程任务ID},
  planId: {类型: bigint, 含义: 关联的行程单ID},
  title: {类型: varchar(128), 含义: 会话标题},
  createTime: {类型: datetime, 含义: 创建时间},
  updateTime: {类型: datetime, 含义: 最后消息时间}
}
```

## 9. 对话消息表（chat_message）
```
{
  msgId: {类型: bigint, 含义: 消息ID，主键},
  sessionId: {类型: bigint, 含义: 所属会话ID},
  userId: {类型: bigint, 含义: 用户ID},
  role: {类型: enum, 含义: 消息角色：user/assistant},
  content: {类型: text, 含义: 消息内容},
  planJson: {类型: json, 含义: 同步更新的行程结构（若有）},
  createTime: {类型: datetime, 含义: 发送时间}
}
```

## 10. 知识库文档表（kb_document）
```
{
  docId: {类型: bigint, 含义: 文档ID，主键，与向量库关联},
  name: {类型: varchar(128), 含义: 文档名称},
  fileType: {类型: varchar(16), 含义: 文件类型：pdf/md/txt},
  tags: {类型: varchar(64), 含义: 文档标签，逗号分隔},
  chunkCount: {类型: int, 含义: 切片数量},
  status: {类型: enum, 含义: 状态：UPLOADING/VECTORIZING/COMPLETED/FAILED},
  createTime: {类型: datetime, 含义: 上传时间}
}
```

## 11. 任务死信/重试记录表（task_retry_deadletter）
```
{
  id: {类型: bigint, 含义: 主键ID},
  taskId: {类型: bigint, 含义: 异常任务ID},
  errorType: {类型: varchar(32), 含义: 异常类型：MQ异常/AI超时/业务异常},
  errorMsg: {类型: text, 含义: 异常详情},
  retryCount: {类型: int, 含义: 已重试次数},
  maxRetry: {类型: int, 含义: 最大重试次数},
  status: {类型: enum, 含义: 状态：WAITING/RETRIED/DEADLETTER},
  createTime: {类型: datetime, 含义: 异常时间},
  updateTime: {类型: datetime, 含义: 最后重试时间}
}
```

## 12. 模型工具调用记录表（tool_call_log）
```
{
  id: {类型: bigint, 含义: 主键ID},
  taskId: {类型: bigint, 含义: 关联任务ID},
  userId: {类型: bigint, 含义: 用户ID},
  modelName: {类型: varchar(64), 含义: 调用的模型名称},
  toolName: {类型: varchar(64), 含义: 工具名称：RAG/搜索/规划},
  promptTokens: {类型: int, 含义: 提示词token数量},
  completionTokens: {类型: int, 含义: 回复token数量},
  totalTokens: {类型: int, 含义: 总token消耗},
  costTime: {类型: int, 含义: 调用耗时(ms)},
  traceId: {类型: varchar(64), 含义: 全链路ID},
  createTime: {类型: datetime, 含义: 调用时间}
}
```







# 旅游AI系统 — **12张表完整关系说明 + 可直接运行的建表语句**
我严格按照你确认的 **12张业务表** 设计，**关系清晰、外键关联、支持JDK17+SpringBoot3**，可直接写论文、直接上线使用。

---

## 一、表关系总览（最简洁易懂版）
1. **用户、角色、菜单**：RBAC权限体系（多对多）
2. **用户 ↔ 行程任务 ↔ 行程规划**：核心业务流程
3. **用户 ↔ 会话 ↔ 消息**：AI对话体系
4. **行程任务 ↔ 死信表 ↔ 模型调用日志**：系统监控
5. **知识库文档表**：独立管理，与向量库关联

---

# 二、完整建表语句（MySQL 8.0 + InnoDB）
## 1. 用户表（user）
```sql
CREATE TABLE `user` (
  `user_id` bigint NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `open_id` varchar(64) NOT NULL COMMENT '微信openId',
  `nickname` varchar(32) DEFAULT NULL COMMENT '昵称',
  `avatar` varchar(512) DEFAULT NULL COMMENT '头像URL',
  `budget` enum('LOW','MEDIUM','HIGH') DEFAULT 'MEDIUM' COMMENT '预算偏好',
  `pace` enum('LEISURE','NORMAL','INTENSE') DEFAULT 'NORMAL' COMMENT '行程节奏',
  `status` tinyint DEFAULT '1' COMMENT '0禁用 1启用',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`user_id`),
  UNIQUE KEY `uk_open_id` (`open_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

## 2. 角色表（role）
```sql
CREATE TABLE `role` (
  `role_id` bigint NOT NULL AUTO_INCREMENT COMMENT '角色ID',
  `role_name` varchar(32) NOT NULL COMMENT '角色名',
  `description` varchar(64) DEFAULT NULL COMMENT '描述',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`role_id`),
  UNIQUE KEY `uk_role_name` (`role_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';
```

## 3. 用户角色关联表（user_role）
```sql
CREATE TABLE `user_role` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `role_id` bigint NOT NULL COMMENT '角色ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_role_id` (`role_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户角色关联表';
```

## 4. 菜单表（menu）
```sql
CREATE TABLE `menu` (
  `menu_id` bigint NOT NULL AUTO_INCREMENT COMMENT '菜单ID',
  `name` varchar(32) NOT NULL COMMENT '菜单名',
  `path` varchar(128) DEFAULT NULL COMMENT '路由',
  `icon` varchar(64) DEFAULT NULL COMMENT '图标',
  `parent_id` bigint DEFAULT '0' COMMENT '父菜单ID',
  `sort` int DEFAULT '0' COMMENT '排序',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`menu_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜单表';
```

## 5. 角色菜单关联表（role_menu）
```sql
CREATE TABLE `role_menu` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `role_id` bigint NOT NULL COMMENT '角色ID',
  `menu_id` bigint NOT NULL COMMENT '菜单ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_role_id` (`role_id`),
  KEY `idx_menu_id` (`menu_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色菜单关联表';
```

## 6. 行程任务请求表（travel_task）
```sql
CREATE TABLE `travel_task` (
  `task_id` bigint NOT NULL AUTO_INCREMENT COMMENT '任务ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `user_query` text COMMENT '用户需求',
  `days` int DEFAULT NULL COMMENT '出行天数',
  `budget` varchar(16) DEFAULT NULL COMMENT '预算',
  `pace` varchar(16) DEFAULT NULL COMMENT '节奏',
  `status` enum('INIT','QUEUED','PROCESSING','COMPLETED','FAILED','CANCELLED') DEFAULT 'INIT',
  `plan_id` bigint DEFAULT NULL COMMENT '行程单ID',
  `error_msg` varchar(512) DEFAULT NULL COMMENT '失败原因',
  `trace_id` varchar(64) DEFAULT NULL COMMENT '全链路ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`task_id`),
  KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='行程任务表';
```

## 7. 行程规划表（travel_plan）
```sql
CREATE TABLE `travel_plan` (
  `plan_id` bigint NOT NULL AUTO_INCREMENT COMMENT '行程单ID',
  `task_id` bigint NOT NULL COMMENT '任务ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `title` varchar(128) NOT NULL COMMENT '标题',
  `destination` varchar(64) NOT NULL COMMENT '目的地',
  `days` int NOT NULL COMMENT '天数',
  `content` json NOT NULL COMMENT '行程JSON',
  `weather` varchar(256) DEFAULT NULL COMMENT '天气',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`plan_id`),
  KEY `idx_task_id` (`task_id`),
  KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='行程规划表';
```

## 8. 会话记录表（chat_session）
```sql
CREATE TABLE `chat_session` (
  `session_id` bigint NOT NULL AUTO_INCREMENT COMMENT '会话ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `task_id` bigint DEFAULT NULL COMMENT '任务ID',
  `plan_id` bigint DEFAULT NULL COMMENT '行程ID',
  `title` varchar(128) DEFAULT '新对话',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`session_id`),
  KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会话记录表';
```

## 9. 对话消息表（chat_message）
```sql
CREATE TABLE `chat_message` (
  `msg_id` bigint NOT NULL AUTO_INCREMENT COMMENT '消息ID',
  `session_id` bigint NOT NULL COMMENT '会话ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `role` enum('USER','ASSISTANT') NOT NULL COMMENT '角色',
  `content` text NOT NULL COMMENT '内容',
  `plan_json` json DEFAULT NULL COMMENT '同步行程',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`msg_id`),
  KEY `idx_session_id` (`session_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='对话消息表';
```

## 10. 知识库文档表（kb_document）
```sql
CREATE TABLE `kb_document` (
  `doc_id` bigint NOT NULL AUTO_INCREMENT COMMENT '文档ID',
  `name` varchar(128) NOT NULL COMMENT '文件名',
  `file_type` varchar(16) NOT NULL COMMENT '文件类型',
  `tags` varchar(64) DEFAULT NULL COMMENT '标签',
  `chunk_count` int DEFAULT '0' COMMENT '切片数',
  `status` enum('UPLOADING','VECTORIZING','COMPLETED','FAILED') DEFAULT 'UPLOADING',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`doc_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识库文档表';
```

## 11. 任务死信/重试记录表（task_retry_deadletter）
```sql
CREATE TABLE `task_retry_deadletter` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `task_id` bigint NOT NULL COMMENT '任务ID',
  `error_type` varchar(32) DEFAULT NULL COMMENT '异常类型',
  `error_msg` text COMMENT '异常信息',
  `retry_count` int DEFAULT '0' COMMENT '重试次数',
  `max_retry` int DEFAULT '3' COMMENT '最大重试',
  `status` enum('WAITING','RETRIED','DEADLETTER') DEFAULT 'WAITING',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_task_id` (`task_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='任务重试/死信表';
```

## 12. 模型工具调用记录表（tool_call_log）
```sql
CREATE TABLE `tool_call_log` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `task_id` bigint DEFAULT NULL COMMENT '任务ID',
  `user_id` bigint DEFAULT NULL COMMENT '用户ID',
  `model_name` varchar(64) DEFAULT NULL COMMENT '模型',
  `tool_name` varchar(64) DEFAULT NULL COMMENT '工具',
  `prompt_tokens` int DEFAULT '0',
  `completion_tokens` int DEFAULT '0',
  `total_tokens` int DEFAULT '0',
  `cost_time` int DEFAULT '0' COMMENT '耗时ms',
  `trace_id` varchar(64) DEFAULT NULL COMMENT '全链路ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_task_id` (`task_id`),
  KEY `idx_trace_id` (`trace_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='模型工具调用日志表';
```

---

# 三、表与表之间关系（最清晰版本）
## 1. 权限体系
- **user ↔ role**：多对多（通过 user_role 关联）
- **role ↔ menu**：多对多（通过 role_menu 关联）

## 2. 行程业务核心
- **user → travel_task**：一个用户可创建多个任务
- **travel_task → travel_plan**：一个任务生成一个行程单

## 3. AI对话
- **user → chat_session**：一个用户多个会话
- **chat_session → chat_message**：一个会话多条消息

## 4. 系统监控
- **travel_task → task_retry_deadletter**：异常任务进入死信
- **travel_task → tool_call_log**：任务产生模型调用日志

## 5. 知识库
- **kb_document**：独立表，与向量库通过 docId 关联

---

## 四、可直接写进论文的【数据库关系说明】
> 系统共设计12张数据表，分为权限管理、行程业务、AI对话、系统监控、知识库五大模块。用户表为核心基础表，通过角色与菜单实现RBAC权限控制；用户可创建多个行程任务，任务生成唯一行程规划；用户可建立多个对话会话，会话包含多条对话消息；行程任务执行异常会进入重试/死信表，并记录模型工具调用日志；知识库文档表独立管理文档信息，与向量数据库通过文档ID关联。整体结构清晰、低耦合、高扩展，满足旅游AI规划系统全业务流程。

---

















# 新数据库表建表语句

## 1. 用户表（user）
```sql
CREATE TABLE `user` (
  `user_id` bigint NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `open_id` varchar(64) NOT NULL COMMENT '微信openId',
  `nickname` varchar(32) DEFAULT NULL COMMENT '昵称',
  `avatar` varchar(512) DEFAULT NULL COMMENT '头像URL',
  `budget` enum('LOW','MEDIUM','HIGH') DEFAULT 'MEDIUM' COMMENT '预算偏好',
  `pace` enum('LEISURE','NORMAL','INTENSE') DEFAULT 'NORMAL' COMMENT '行程节奏',
  `status` tinyint DEFAULT '1' COMMENT '0禁用 1启用',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`user_id`),
  UNIQUE KEY `uk_open_id` (`open_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

## 2. 角色表（role）
```sql
CREATE TABLE `role` (
  `role_id` bigint NOT NULL AUTO_INCREMENT COMMENT '角色ID',
  `role_name` varchar(32) NOT NULL COMMENT '角色名',
  `description` varchar(64) DEFAULT NULL COMMENT '描述',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`role_id`),
  UNIQUE KEY `uk_role_name` (`role_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';
```

## 3. 用户角色关联表（user_role）【**补充外键**】
```sql
CREATE TABLE `user_role` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `role_id` bigint NOT NULL COMMENT '角色ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_role_id` (`role_id`),
  -- 外键约束
  CONSTRAINT `fk_user_role_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`user_id`) ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT `fk_user_role_role` FOREIGN KEY (`role_id`) REFERENCES `role` (`role_id`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户角色关联表';
```

## 4. 菜单表（menu）
```sql
CREATE TABLE `menu` (
  `menu_id` bigint NOT NULL AUTO_INCREMENT COMMENT '菜单ID',
  `name` varchar(32) NOT NULL COMMENT '菜单名',
  `path` varchar(128) DEFAULT NULL COMMENT '路由',
  `icon` varchar(64) DEFAULT NULL COMMENT '图标',
  `parent_id` bigint DEFAULT '0' COMMENT '父菜单ID',
  `sort` int DEFAULT '0' COMMENT '排序',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`menu_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜单表';
```

## 5. 角色菜单关联表（role_menu）【**补充外键**】
```sql
CREATE TABLE `role_menu` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `role_id` bigint NOT NULL COMMENT '角色ID',
  `menu_id` bigint NOT NULL COMMENT '菜单ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_role_id` (`role_id`),
  KEY `idx_menu_id` (`menu_id`),
  -- 外键约束
  CONSTRAINT `fk_role_menu_role` FOREIGN KEY (`role_id`) REFERENCES `role` (`role_id`) ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT `fk_role_menu_menu` FOREIGN KEY (`menu_id`) REFERENCES `menu` (`menu_id`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色菜单关联表';
```

## 6. 行程任务请求表（travel_task）【**补充外键**】
```sql
CREATE TABLE `travel_task` (
  `task_id` bigint NOT NULL AUTO_INCREMENT COMMENT '任务ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `user_query` text COMMENT '用户需求',
  `days` int DEFAULT NULL COMMENT '出行天数',
  `budget` varchar(16) DEFAULT NULL COMMENT '预算',
  `pace` varchar(16) DEFAULT NULL COMMENT '节奏',
  `status` enum('INIT','QUEUED','PROCESSING','COMPLETED','FAILED','CANCELLED') DEFAULT 'INIT',
  `plan_id` bigint DEFAULT NULL COMMENT '行程单ID',
  `error_msg` varchar(512) DEFAULT NULL COMMENT '失败原因',
  `trace_id` varchar(64) DEFAULT NULL COMMENT '全链路ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`task_id`),
  KEY `idx_user_id` (`user_id`),
  -- 外键约束
  CONSTRAINT `fk_travel_task_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`user_id`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='行程任务表';
```

## 7. 行程规划表（travel_plan）【**补充外键**】
```sql
CREATE TABLE `travel_plan` (
  `plan_id` bigint NOT NULL AUTO_INCREMENT COMMENT '行程单ID',
  `task_id` bigint NOT NULL COMMENT '任务ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `title` varchar(128) NOT NULL COMMENT '标题',
  `destination` varchar(64) NOT NULL COMMENT '目的地',
  `days` int NOT NULL COMMENT '天数',
  `content` json NOT NULL COMMENT '行程JSON',
  `weather` varchar(256) DEFAULT NULL COMMENT '天气',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`plan_id`),
  KEY `idx_task_id` (`task_id`),
  KEY `idx_user_id` (`user_id`),
  -- 外键约束
  CONSTRAINT `fk_travel_plan_task` FOREIGN KEY (`task_id`) REFERENCES `travel_task` (`task_id`) ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT `fk_travel_plan_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`user_id`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='行程规划表';
```

## 8. 会话记录表（chat_session）【**补充外键**】
```sql
CREATE TABLE `chat_session` (
  `session_id` bigint NOT NULL AUTO_INCREMENT COMMENT '会话ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `title` varchar(128) DEFAULT '新对话',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`session_id`),
  KEY `idx_user_id` (`user_id`),
  -- 外键约束
  CONSTRAINT `fk_chat_session_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`user_id`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会话记录表';
```

## 9. 对话消息表（chat_message）【**补充外键**】
```sql
CREATE TABLE `chat_message` (
  `msg_id` bigint NOT NULL AUTO_INCREMENT COMMENT '消息ID',
  `session_id` bigint NOT NULL COMMENT '会话ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `role` enum('USER','ASSISTANT') NOT NULL COMMENT '角色',
  `content` text NOT NULL COMMENT '内容',
  `plan_json` json DEFAULT NULL COMMENT '同步行程',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`msg_id`),
  KEY `idx_session_id` (`session_id`),
  -- 外键约束
  CONSTRAINT `fk_chat_message_session` FOREIGN KEY (`session_id`) REFERENCES `chat_session` (`session_id`) ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT `fk_chat_message_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`user_id`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='对话消息表';
```

## 10. 知识库文档表（kb_document）
无外键
```sql
CREATE TABLE `kb_document` (
  `doc_id` bigint NOT NULL AUTO_INCREMENT COMMENT '文档ID',
  `name` varchar(128) NOT NULL COMMENT '文件名',
  `file_type` varchar(16) NOT NULL COMMENT '文件类型',
  `tags` varchar(64) DEFAULT NULL COMMENT '标签',
  `chunk_count` int DEFAULT '0' COMMENT '切片数',
  `status` enum('UPLOADING','VECTORIZING','COMPLETED','FAILED') DEFAULT 'UPLOADING',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`doc_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识库文档表';
```

## 11. 任务死信/重试记录表（task_retry_deadletter）【**补充外键**】
```sql
CREATE TABLE `task_retry_deadletter` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `task_id` bigint NOT NULL COMMENT '任务ID',
  `error_type` varchar(32) DEFAULT NULL COMMENT '异常类型',
  `error_msg` text COMMENT '异常信息',
  `retry_count` int DEFAULT '0' COMMENT '重试次数',
  `max_retry` int DEFAULT '3' COMMENT '最大重试',
  `status` enum('WAITING','RETRIED','DEADLETTER') DEFAULT 'WAITING',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_task_id` (`task_id`),
  -- 外键约束
  CONSTRAINT `fk_task_retry_deadletter_task` FOREIGN KEY (`task_id`) REFERENCES `travel_task` (`task_id`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='任务重试/死信表';
```

## 12. 模型工具调用记录表（tool_call_log）【**补充外键**】
```sql
CREATE TABLE `tool_call_log` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `task_id` bigint DEFAULT NULL COMMENT '任务ID',
  `user_id` bigint DEFAULT NULL COMMENT '用户ID',
  `model_name` varchar(64) DEFAULT NULL COMMENT '模型',
  `tool_name` varchar(64) DEFAULT NULL COMMENT '工具',
  `prompt_tokens` int DEFAULT '0',
  `completion_tokens` int DEFAULT '0',
  `total_tokens` int DEFAULT '0',
  `cost_time` int DEFAULT '0' COMMENT '耗时ms',
  `trace_id` varchar(64) DEFAULT NULL COMMENT '全链路ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_task_id` (`task_id`),
  KEY `idx_trace_id` (`trace_id`),
  -- 外键约束
  CONSTRAINT `fk_tool_call_log_task` FOREIGN KEY (`task_id`) REFERENCES `travel_task` (`task_id`) ON DELETE SET NULL ON UPDATE CASCADE,
  CONSTRAINT `fk_tool_call_log_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`user_id`) ON DELETE SET NULL ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='模型工具调用日志表';
```







