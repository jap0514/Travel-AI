我直接给你**最稳妥、最不容易翻车、最适合毕业设计/项目开发**的编码顺序，按**天/阶段**划分，你照着做就能一步步跑通全流程，不会卡壳。

# 旅游AI系统 —— 开发顺序（终极版·可直接执行）
## 总原则
1. **先搭环境 → 再跑通流程 → 再加功能 → 最后优化**
2. **前端和后端并行，但后端先出接口**
3. **WebSocket + MQ 异步流程必须单独验证**
4. **AI 部分最后做，先用假数据跑通全链路**

---

# 阶段 0：环境搭建（第 1 天）
**必须最先做，不然后面全卡**
1. 安装 JDK17 + Maven + MySQL8 + Redis7 + RabbitMQ
2. 新建 SpringBoot3 项目，导入依赖（你文档里的 pom.xml）
3. 跑通：启动类、Swagger/Knife4j、统一返回体、全局异常
4. 执行 12 张建表 SQL
5. Python 环境安装：requirements.txt
6. 微信小程序开发工具初始化

**输出**
- 后端能启动
- 数据库 12 张表建好
- Redis/RabbitMQ 可连接
- 小程序空白项目建好

---

# 阶段 1：基础架构（第 1–2 天）
**所有业务的地基**
1. 统一响应体 + 全局异常
2. JWT + SpringSecurity 权限拦截
3. TraceID 全链路日志
4. MyBatis-Plus 配置 + 代码生成（entity/mapper/service）
5. Redis 工具类 + 限流工具类
6. RabbitMQ 配置（交换机、队列、收发消息）

**输出**
- 登录鉴权可用
- 日志可追踪
- MQ 收发消息测试通过
- 数据库 CRUD 可用

---

# 阶段 2：用户模块（第 2 天）
**最简单、最容易先写完**
1. 微信登录接口（code → openId → 自动注册）
2. 获取个人信息、修改信息
3. 管理员：用户列表、禁用用户
4. 小程序端：登录页、个人中心

**输出**
- 小程序能登录、拿到 Token
- 管理员能看用户

---

# 阶段 3：核心任务流程（第 3–4 天 · 最重要）
**先不调 AI，用假数据跑通整个异步流程**

## 后端（优先）
1. 任务表 CRUD
2. **创建任务接口**（/task/create）
3. Java 发送消息到 RabbitMQ
4. Java 监听 MQ 结果队列
5. WebSocket 配置（STOMP）
6. 任务状态机：INIT → QUEUED → PROCESSING → COMPLETED
7. 任务状态查询接口
8. 管理员重试/取消任务

## 小程序端
1. 输入页（文字/语音）
2. 提交需求 → 拿到 taskId
3. WebSocket 连接 + 订阅进度
4. 进度页面展示

**关键验证（必须成功）**
提交需求 → MQ 发消息 → WebSocket 推进度 → 推假结果 → 展示行程

---

# 阶段 4：行程存储与展示（第 4–5 天）
**任务跑完就要存结果**
1. 行程表（travel_plan）结构化存储
2. 获取行程详情接口
3. 我的历史行程列表接口
4. 删除行程接口
5. 小程序行程详情页、历史页

**输出**
- 假行程能正常展示、保存、删除
- 历史列表分页正常

---

# 阶段 5：AI 对话（追问）（第 5 天）
1. 会话表 + 消息表
2. 发送对话接口
3. 聊天历史、会话列表
4. 小程序对话页面

**先不用真 AI，返回固定文本即可**

---

# 阶段 6：Python AI 服务接入（第 6–7 天）
**前面全通了，再接入真 AI，绝对不慌**
1. Python 连接 RabbitMQ
2. Python 消费任务消息
3. 大模型调用（固定返回也行）
4. RAG 检索（可选）
5. 格式化 JSON 结果
6. Python 发送结果回 MQ
7. Java 接收并入库

**输出**
- 从提交需求 → AI 生成 → 推送结果 → 展示全流程通了

---

# 阶段 7：管理员后台（第 7–8 天）
1. 角色/菜单权限
2. 系统健康检查
3. 日志查询（traceId）
4. 业务统计看板
5. 知识库上传、向量化、检索测试
6. Redis/向量库管理
7. 动态修改 AI 配置

---

# 阶段 8：异常、重试、死信、优化（第 8–9 天）
**项目稳定性，毕设加分项**
1. MQ 重试机制
2. 死信队列
3. 任务超时（Redis TTL）
4. 接口限流
5. 幂等校验
6. 前端断网重连、重连机制
7. 失败重试按钮

---

# 阶段 9：测试 + 联调 + 部署（第 9–10 天）
1. 接口测试
2. 小程序全流程走通
3. 管理后台功能验证
4. 服务器部署（Java + Python + MySQL + Redis + MQ）
5. 域名 + HTTPS + 小程序上线配置

---

# 最简开发口诀（你照着念就行）
1. **先搭环境**
2. **再做登录**
3. **再做任务提交**
4. **再做 MQ + WebSocket 假数据跑通**
5. **再做行程展示**
6. **最后接入 Python AI**
7. **最后补管理后台**

---

# 你每天要做的最简清单（直接照做）
## Day1
环境 + 架构 + 数据库

## Day2
登录 + 用户模块

## Day3
任务创建 + MQ + WebSocket（假数据）

## Day4
行程存储 + 历史 + 详情

## Day5
AI 对话模块

## Day6
Python AI 接入

## Day7
管理后台 + 监控

## Day8
异常、重试、死信、优化

## Day9
部署 + 测试 + 验收

---

如果你愿意，我可以**直接给你生成：**
- 每个阶段的**代码目录结构**
- 每个接口的**Controller/Service/DAO 代码**
- **WebSocket + MQ 完整可运行代码**
- Python 消费任务的**模板代码**

你要我直接开始生成**阶段 1 的代码**吗？