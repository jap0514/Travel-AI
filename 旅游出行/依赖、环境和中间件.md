# 旅游AI系统 — 完整依赖（JDK 17 + SpringBoot 3.2.x）
## 一、全局版本（pom.xml 顶层）
```xml
<properties>
    <java.version>17</java.version>
    <maven.compiler.source>17</maven.compiler.source>
    <maven.compiler.target>17</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <spring.boot.version>3.2.5</spring.boot.version>
    <spring.cloud.version>2023.0.1</spring.cloud.version>
    <mybatis.plus.version>3.5.5</mybatis.plus.version>
    <jjwt.version>0.11.5</jjwt.version>
    <hutool.version>5.8.27</hutool.version>
    <fastjson2.version>2.0.32</fastjson2.version>
</properties>
```

---

# 二、核心依赖（直接复制进 pom.xml）
## 1. SpringBoot 基础
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>

<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
</dependency>

<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis</artifactId>
</dependency>

<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-amqp</artifactId>
</dependency>

<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-websocket</artifactId>
</dependency>

<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-logging</artifactId>
</dependency>

<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-validation</artifactId>
</dependency>
```

---

## 2. MySQL + MyBatis-Plus（JDK17 专用）
```xml
<dependency>
    <groupId>com.baomidou</groupId>
    <artifactId>mybatis-plus-boot-starter</artifactId>
    <version>${mybatis.plus.version}</version>
</dependency>

<dependency>
    <groupId>mysql</groupId>
    <artifactId>mysql-connector-j</artifactId>
    <scope>runtime</scope>
</dependency>
```

---

## 3. JWT 认证（**Jakarta 版，适配 JDK17+**）
```xml
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-api</artifactId>
    <version>${jjwt.version}</version>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-impl</artifactId>
    <version>${jjwt.version}</version>
    <scope>runtime</scope>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-jackson</artifactId>
    <version>${jjwt.version}</version>
    <scope>runtime</scope>
</dependency>
```

---

## 4. 工具库
```xml
<dependency>
    <groupId>cn.hutool</groupId>
    <artifactId>hutool-all</artifactId>
    <version>${hutool.version}</version>
</dependency>

<dependency>
    <groupId>com.alibaba.fastjson2</groupId>
    <artifactId>fastjson2</artifactId>
    <version>${fastjson2.version}</version>
</dependency>
```

---

# 三、Python AI 服务依赖（requirements.txt）
完全兼容你的 Java 后端
```txt
Python >= 3.10

langchain==0.2.0
langchain-openai==0.1.7
openai==1.35.2
pymilvus==2.4.0
faiss-cpu==1.8.0
PyPDF2==3.0.1
pika==1.3.2
redis==5.0.7
pymysql==1.1.0
loguru==0.7.2
python-dotenv==1.0.1
```

---

# 四、中间件版本（JDK17 最佳配套）
```
MySQL 8.0
Redis 7.2
RabbitMQ 3.13
Milvus 2.4
Nginx 1.25
```

---

# 五、最终技术栈（完美稳定版）
- **JDK：17**
- **SpringBoot：3.2.5**
- **Spring Security：6.x**
- **MyBatis-Plus：3.5.5**
- **JWT：0.11.5**
- **MySQL：8.0**
- **Redis：7.2**
- **RabbitMQ：3.13**

---




# 旅游AI系统 —— **JDK17 完整适配环境 + 中间件版本**
全部**稳定、无坑、可直接部署上线**，严格匹配 SpringBoot 3.2.x + JDK17

---

## 一、运行环境（服务器/本地）
```
JDK：17.0.10（LTS）
Maven：3.8.6 / 3.9.6+
Python：3.10.12 / 3.11.7（AI后端）
OS：Linux CentOS 7 / Ubuntu 20.04 / 22.04
```

---

## 二、中间件版本（**JDK17 + SpringBoot3 最佳兼容**）
### 1. MySQL（必选）
```
版本：8.0.36
驱动：mysql-connector-j（已内置在依赖）
字符集：utf8mb4
时区：UTC +8
```

### 2. Redis（必选）
```
版本：7.2.4 / 7.0.15
端口：6379
数据持久化：RDB + AOF
```

### 3. RabbitMQ（必选，任务异步）
```
版本：3.13.0 / 3.12.14
依赖 Erlang：26.x / 25.3
端口：5672 / 15672（控制台）
```

### 4. 向量数据库（RAG知识库）
**二选一即可**
- Milvus（生产推荐）：**2.4.0**
- FAISS（开发/轻量）：**1.8.0**
- Qdrant

### 5. Nginx（反向代理）
```
版本：1.24.0 / 1.25.4
```

---

## 三、SpringBoot 3 + JDK17 兼容说明（关键！）
1. **Jakarta 取代 javax**（所有包名已自动适配）
2. **Spring Security 6.x** 权限配置写法更新
3. **JDK17 不需要模块化参数**
4. **日志、WebSocket、Redis、RabbitMQ 全部自动兼容**
5. **MyBatis-Plus 3.5.5+ 完美支持 JDK17**

---

## 四、完整可直接部署的环境清单（复制给运维）
```
# 语言环境
JDK 17
Python 3.10+
Maven 3.8.6+

# 中间件
MySQL 8.0.36
Redis 7.2.4
RabbitMQ 3.13.0
Milvus 2.4.0
Nginx 1.24.0
```

---