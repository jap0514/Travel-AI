<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.travel.mapper.StatisticsMapper">


    <select id="getUserDestinations" resultType="com.travel.vo.StatisticsUserDestinationVO">
        select
            destination,
            COUNT(*) as count
        from travel_parse_plan
        where user_id=#{pathUserId}
        GROUP BY destination
        ORDER BY count DESC
    </select>

    <select id="getTotalDestinations" resultType="com.travel.vo.StatisticsDestinationVO">
        select
            destination,
            COUNT(*) as count
        from travel_parse_plan
        where create_time >= #{days}
        group by destination
        order by count DESC
    </select>
</mapper>